{
  "name": "wallet-sdk",
  "version": "0.1.0",
  "description": "BYOC SDK for W3DS wallet flows (provision, auth, sync, sign)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vitest": "^2.1.0"
  },
  "private": true
}
