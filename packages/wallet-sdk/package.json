{
  "name": "wallet-sdk",
  "version": "0.1.0",
  "description": "BYOC SDK for W3DS wallet flows (provision, auth, sync, sign)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "private": true
}
